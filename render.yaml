# render.yaml — Deploy cả FastAPI Web Service + Celery Worker trên Render.com
# Docs: https://render.com/docs/blueprint-spec
#
# Cách dùng:
#   1. Push repo lên GitHub
#   2. Vào render.com → New → Blueprint
#   3. Kết nối GitHub repo → Render tự đọc file này và tạo cả 2 service

services:
  # ── 1. FastAPI Web Service (API endpoints) ────────────────────────────────
  - type: web
    name: it-job-finder-ai
    runtime: python
    region: singapore   # Gần Việt Nam nhất
    plan: free

    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT

    # Health check — Render dùng để biết service đã sẵn sàng chưa
    healthCheckPath: /health

    envVars:
      # ── App ──────────────────────────────────────────────────────────────
      - key: APP_ENV
        value: production

      # ── JWT (phải giống Node.js server) ──────────────────────────────────
      - key: ACCESS_TOKEN_SECRET
        sync: false   # Set thủ công trên Render dashboard (secret)

      # ── MongoDB Atlas (cluster mới cho CV/chat) ───────────────────────────
      - key: MONGODB_URI
        sync: false   # Set thủ công: mongodb+srv://user:pass@cluster.mongodb.net

      - key: MONGODB_DB
        value: cv_chatbot

      # ── MongoDB Atlas (cùng cluster Node.js server, đọc jobs) ─────────────
      - key: JOBS_MONGO_URI
        sync: false   # Set thủ công: URI Atlas cluster của Node.js

      - key: JOBS_MONGO_DB
        value: ITJOBS

      # ── Upstash Redis ─────────────────────────────────────────────────────
      - key: REDIS_URL
        sync: false   # Set thủ công: rediss://...upstash.io:6379

      # ── Gemini API ────────────────────────────────────────────────────────
      - key: GEMINI_API_KEY
        sync: false

      # ── Groq API ─────────────────────────────────────────────────────────
      - key: GROQ_API_KEY
        sync: false

  # ── 2. Celery Background Worker (xử lý CV embedding) ─────────────────────
  - type: worker
    name: it-job-finder-ai-worker
    runtime: python
    region: singapore
    plan: free

    buildCommand: pip install -r requirements.txt
    startCommand: celery -A workers.celery_app worker --loglevel=info --concurrency=1

    envVars:
      # Celery Worker cần cùng config như FastAPI để kết nối Redis + MongoDB
      - key: APP_ENV
        value: production

      - key: ACCESS_TOKEN_SECRET
        sync: false

      - key: MONGODB_URI
        sync: false

      - key: MONGODB_DB
        value: cv_chatbot

      - key: JOBS_MONGO_URI
        sync: false

      - key: JOBS_MONGO_DB
        value: ITJOBS

      - key: REDIS_URL
        sync: false

      - key: GEMINI_API_KEY
        sync: false

      - key: GROQ_API_KEY
        sync: false
